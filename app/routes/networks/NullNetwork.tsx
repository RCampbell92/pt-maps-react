import React from "react";

const NullNetwork = () => {
    return (
        <div className="network">
            <p>There is no network selected. Choose from the dropdown above.</p>
        </div>
    );
};

export default NullNetwork;
